name: Ruby

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    name: Ruby ${{ matrix.ruby-version }} on ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ ubuntu-latest ]
        ruby-version: [ '3.3', 'latest', 'head', 'truffleruby' ]
    runs-on: ${{ matrix.os }}
    env:
      FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true
    steps:
      - uses: actions/checkout@v6
      - name: Set up Ruby
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: ${{ matrix.ruby-version }}
          bundler-cache: true
      - name: Run rspec
        run: bundle exec rspec
      - name: Run cucumber
        run: bundle exec cucumber
      - name: Run rubocop
        run: bundle exec rubocop --config .rubocop.yml
